fn main() {
    let mut x = 5;  // x is mutable
    let x = x;      // x is now immutable
}

fn main2() {
    let meme = "More cowbell!";
    let meme = make_image(meme); // meme starts as text the becomes an image name
